# Application
NODE_ENV=development
PORT=3000
API_VERSION=v1
API_PREFIX=api/v1

# CORS
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# Database
# Development (SQLite)
DATABASE_TYPE=sqlite
DATABASE_NAME=./data/tradingpool.db
DATABASE_SYNC=true
DATABASE_LOGGING=false

# Production (PostgreSQL)
# DATABASE_TYPE=postgres
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=trading_pool
# DATABASE_USER=postgres
# DATABASE_PASSWORD=postgres
# DATABASE_SYNC=false

# Redis (Optional - for now we'll use memory)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT
JWT_ACCESS_SECRET=change_this_to_a_very_long_random_secret_for_access_tokens
JWT_REFRESH_SECRET=change_this_to_a_very_long_random_secret_for_refresh_tokens
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d

# Encryption
MASTER_ENCRYPTION_KEY=change_this_to_32_character_key

# Admin Credentials
ADMIN_EMAIL=sesshomaru@admin.com
ADMIN_PASSWORD=inyasha
ADMIN_USERNAME=sesshomaru

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# SMTP (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@tradingpool.com

# Trading Module (Python) - Mock for now
TRADING_MODULE_URL=http://localhost:4000
TRADING_API_KEY=mock_trading_api_key

# Monitoring
SENTRY_DSN=
